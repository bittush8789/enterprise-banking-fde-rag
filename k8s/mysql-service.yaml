apiVersion: v1
kind: Service
metadata:
  name: bankassist-mysql
  namespace: bankassist-ai
  labels:
    app.kubernetes.io/name: bankassist-mysql
    app.kubernetes.io/part-of: bankassist-ai
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app.kubernetes.io/name: bankassist-mysql
